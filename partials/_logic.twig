layout:
    # standard, album, cover, carousel, blog, dashboard
    style: standard
    #  container / container-fluid
    width: default

# ?????
singlepage: default


navbar:
    # hide / normal/ class:fixed-top
    position: default
    # wrapper: container / container-fluid
    width: default
    # style tag
    alignment: default

footer:
    # show / hide
    sticky: default

    # show / hide
offcanvas: default

# default (true) or false
search: show / hide
